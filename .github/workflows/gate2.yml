name: Gate 2 fast-memory audit

on:
  push:
    branches: [gate2-fast-memory]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  gate2:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m pip install -r requirements.txt
      - name: Unit tests
        run: pytest -q
      - name: Matched-cost Gate 1 PING controls
        run: python gate1_probe_controls.py --start 1000 --n 100 --out /tmp/gate1_matched.json
      - name: Development smoke
        run: python gate2_fast_memory.py --start 0 --n 20 --out /tmp/gate2_dev.json
      - name: Held-out audit
        run: python gate2_fast_memory.py --start 1000 --n 100 --out /tmp/gate2_heldout.json
      - name: Recurrence and close-context attacks
        run: python gate2_attacks.py --start 1000 --n 100 --out /tmp/gate2_attacks.json
